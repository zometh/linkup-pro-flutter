import 'package:flutter/material.dart';
class MessagesHome extends StatelessWidget {
  const MessagesHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Messages Home Page'),
      ),
    );
  }
}
